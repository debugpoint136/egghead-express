<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>User Index</title>
    <style media="screen">
      * {
        font-family: Helvetica, Arial, sans-serif;
      }

      div {
        display: flex;
        align-items: center;
        padding: 1em;
        font-size: 1.2em;
      }

      div a {
        padding-left: 0.5em;
      }

      img {
        width: 150px;
        height: 170px;
      }
    </style>
  </head>
  <body>

    <h1> <small>Handlebars</small></h1>

    <ul>
      {{#each users as |user|}}
      <div>
        <img src="/profilepics/{{user.username}}_sm.jpeg" alt="" />
        <a href="/{{user.username}}">{{user.name}}</a>
      </div>
      {{/each}}
    </ul>

  </body>
</html>
